<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
	
	<resultMap type="Board" id="boardMap">
		<result column="pk" property="pk"/>
		<result column="title" property="title"/>
		<result column="description" property="description"/>
		<result column="image" property="image"/>
		<result column="good" property="good"/>
		<result column="bad" property="bad"/>
		<result column="hit" property="hit"/>
		<result column="tag" property="tag"/>
		<result column="email" property="email"/>
		<result column="create_date" property="create_date"/>		
	</resultMap>
	
	<insert id="insertBoard" parameterType="Board">
		insert into board(pk, title, description, create_date) values(board_seq.nextval, #{title},
		#{description}, sysdate)	
	</insert>
	
	<insert id="tagInsert" parameterType="com.model.Tag">
		insert into tags values (#{board_pk}, #{member_pk}, #{name})
	</insert>
	
	<select id="selectAll" resultMap="boardMap">
		select * from board
	</select>
	
	<select id="selectCount" resultType="Integer">
		select count(*) from board_cafe
	</select>
	
	<select id="getBoardPk" resultType="Integer">
		select board_seq.currval from dual
	</select>
	
	<select id="boardList"  parameterType="int"  resultMap="boardMap">
	    <![CDATA[
		select * from
		(select rownum rnum,pk,title,description,image,good,bad,hit,tag,create_date from
		(select * from board_cafe order by create_date desc))
		where rnum >= ((#{page}-1)*10+1) and rnum <= (#{page}*10)
		]]>		
	</select>
	
	<select id="getBoard" resultMap="boardMap" parameterType="int">
		select * from board_cafe where pk=#{id}
	</select>	
	
</mapper>