<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
	
	<resultMap type="Board" id="boardMap">
		<result column="pk" property="pk"/>
		<result column="title" property="title"/>
		<result column="description" property="description"/>
		<result column="image" property="image"/>
		<result column="good" property="good"/>
		<result column="bad" property="bad"/>
		<result column="hit" property="hit"/>
		<result column="tag" property="tag"/>
		<result column="create_date" property="create_date"/>
		<result column="email" property="email"/>
	</resultMap>
	
	<insert id="insert" parameterType="Board">
		insert into board_cafe values(board_seq.nextval, #{title},
		#{description}, #{image}, 0, 0, 0, #{tag},'aa@aa', sysdate)	
	</insert>
	<select id="selectAll" resultMap="boardMap">
		select * from board_cafe
	</select>
	<select id="selectTag" resultType="arraylist">
		select * from board_cafe
	</select>
</mapper>